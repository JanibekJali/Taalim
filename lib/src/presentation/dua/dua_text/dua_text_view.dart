import 'package:flutter/material.dart';

class DuaTextView extends StatelessWidget {
  const DuaTextView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Dua Text View'),
      ),
    );
  }
}
